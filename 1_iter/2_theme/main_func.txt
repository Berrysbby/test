1)Напишите пустую функцию main с сигнатурой void, постройте и запустите ее.
Чем объясняется такое поведение? 
2)
аписать программу, которая:

принимает 1 аргумент: строку-команду ("ok", "fail", "usage")
в зависимости от аргумента завершает программу разными кодами (например 0, 1, 2)
а если аргумент не передан — печатает подсказку и возвращает отдельный код
В задаче нужно отдельно ответить (после реализации):

чем отличается return 0; от return EXIT_SUCCESS;
какие значения “принято” возвращать и почему
что увидит оболочка/скрипт (shell) как “успех/ошибка”

3)Напишите программу expr, которая вычисляла бы выражение в обратной польской записи из командной строки.
Каждый операнд и знак операции должен быть отдельным аргументом.
После выражения напишите результат вычисление и количество аргументов командной строки.
Обязательно корректно обработайте ситуацию, когда пользователь передал лишние аргументы.

4)
Сделай программу, которая для каждого аргумента печатает:
индекс
строку
длину (через strlen)
первые и последние 3 символа (если длина позволяет)
Дополнительно: выведи адреса argv[i] (как указатели), чтобы увидеть, что именно хранится в argv.

5)
Напиши программу, которая показывает, что можно выполнить код до main, используя только стандартные/обычные для Linux средства C.

Вариант задания:

напиши функцию, которая печатает строку
добейся, чтобы она выполнилась до первого принта из main (например, через атрибут конструктора, если доступен в GCC/Clang)
в main тоже печатай строки, чтобы по выводу был виден порядок




6)Сделать программу, которая демонстрирует порядок событий:

глобальный объект/переменная с “конструктором” (в C++), который печатает сообщение
локальный static объект в функции, печатающий при инициализации
atexit() (или аналог) для печати при завершении
печать из main

7)
Сделай утилиту:

bash
./tool [-a] [-b ARG] [-n NUM] -- file1 file2 ...
Требования:

использовать getopt() (POSIX)
поддержать -- (конец опций)
после разбора вывести:
какие флаги включены
значения параметров
индекс optind и список оставшихся позиционных аргументов
Цель: научиться правильно подключать заголовки, понимать контракты getopt и интерпретировать optind/optarg.

8)
Сделай программу, которая:
регистрирует 2–3 функции через atexit(), каждая печатает сообщение
в зависимости от аргумента командной строки завершает работу одним из способов:
обычный return из main
exit(code)
_Exit(code) (или _exit(code))
Нужно вывести, какие сообщения реально печатаются при каждом способе завершения.
